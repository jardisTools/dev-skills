# jardisadapter/fakecache — Agent Notes

Fixture package for E2E tests. The plugin should aggregate this body into the consumer's AGENTS.md.

## Marker

`FAKE_VENDOR_AGENTS_BODY_MARKER` — the E2E test asserts on this string.
